# Dépendances
node_modules/

# Secrets — la clé API n'est JAMAIS commitée
.env
.env.local

# Sorties du pipeline (PDF de test + JSON générés)
pipeline/invoices/*
!pipeline/invoices/.gitkeep
pipeline/output/*
!pipeline/output/.gitkeep

# Build
dist/
*.tsbuildinfo

# Outils de développement (réinstallables, hors livrable)
# Le skill de design Impeccable : npx skills add pbakaus/impeccable
.agents/
.claude/
skills-lock.json

# Divers
.DS_Store
