{
  "name": "juwa-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "B1 — Extraction structurée de factures PDF (OCR Mistral -> JSON validé Zod)",
  "scripts": {
    "extract": "tsx src/cli.ts",
    "serve": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/verify.ts"
  },
  "dependencies": {
    "@mistralai/mistralai": "^1.5.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
